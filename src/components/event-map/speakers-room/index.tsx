import classes from "./speakers-room.module.scss";

const SpeakersRoom: React.FC = () => (
  <div className={classes.container}>
    <p className={classes.text}>SPEAKERS ROOM</p>
  </div>
);

export default SpeakersRoom;
